import styled from 'styled-components';

export const Container = styled.span`
  font-size: 1.8rem;
  font-weight: 500;
  font-style: italic;
  text-align: center;
  color: var(--dark-gray);
  padding: 5rem 0;
`;
